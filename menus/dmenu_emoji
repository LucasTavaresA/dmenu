#!/bin/sh

chosen=$(cut -d ';' -f1 $HOME/dmenu/menus/listas/emojis | dmenu -F -l 35 -c -z 200 | sed "s/ .*//")

# Sai caso nenhum é selecionado.
[ -z "$chosen" ] && exit

if [ -n "$1" ]; then
	xdotool type "$chosen"
else
	printf "$chosen" | xclip -selection clipboard
	notify-send "'$chosen' copiado." &
    xdotool type --window "$window_id" "$chosen"
fi
