#!/bin/sh

select=$(VBoxManage list vms \
    | cut -d '"' -f2 \
    | dmenu -l 35 -h 20 -c -cw 100 -p "vm" \
)

[ -n "$select" ] \
    && notify-send \
        "virtualbox" \
        "starting $select" \
    && VBoxManage startvm "$select" >/dev/null 2>&1